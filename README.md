# Forkop X

[![Releases](https://img.shields.io/github/v/release/slayer326/forkop?label=releases)](https://github.com/slayer326/forkop/releases)

Прозрачная маршрутизация трафика на роутерах OpenWrt: sing-box, списки маршрутизации, подписки, обход DPI.

> **Forkop — это бывший Podkop Plus.** Проект переименован и развивается как независимый форк [Podkop](https://github.com/itdoginfo/podkop).
> Эта сборка — продолжение [Forkop](https://github.com/ushan0v/forkop) с упором на то, чтобы установка и обновление доходили до конца, а не падали на полпути.

## Установка

```sh
wget -qO- https://fold8.ru/forkop/install.sh | sh
```

Одна команда на свежем роутере — установщик сам подтянет зависимости, ядро и списки. Поддерживаются OpenWrt 24.10 и 25.12+ на платформах Filogic (`aarch64_cortex-a53`) и Rockchip (`aarch64_generic`).

Установщик и встроенное обновление работают через канал `https://fold8.ru/forkop`. Если его метаданные недоступны, проверка обновлений переключается на GitHub Releases.

## Чем отличается от оригинального Forkop

### Своя инфраструктура вместо чужих ссылок

Оригинал полагается на сторонние источники: пакеты OpenWrt, списки маршрутизации, релизы. Если что-то из этого недоступно — установка обрывается на середине, а роутер остаётся в непонятном состоянии.

Здесь работает собственное зеркало со всеми зависимостями, ядром OpenWrt и списками маршрутизации. Зеркало публикует индекс готовых платформ, и установщик **отказывается менять репозитории пакетов**, пока не убедится, что нужная комбинация версии и архитектуры там действительно есть.

### Загрузки не падают от одной неудачи

Каждый источник данных имеет резерв:

- **Списки и наборы правил** — при недоступности зеркала переключаются на jsDelivr, затем на GitHub. Работает и для текстовых списков, и для бинарных `.srs`.
- **DNS** — если системный резолвер не отвечает, загрузка идёт через резервный DNS. При настроенном прокси загрузка остаётся через него, а не в обход.
- **Кэш** — при любой ошибке сохраняется последняя рабочая версия списков. Обновление применяется целиком либо не применяется вовсе.
- **Холодная загрузка** — после перезагрузки роутера обновление списков ждёт появления сети, а не сдаётся на первой попытке.

### Обновление доводится до конца

Самая частая жалоба на подобные проекты: «обновился, и всё отвалилось». Здесь закрыты три отдельные причины этого:

- `opkg` на части сборок OpenWrt 24 вызывает скрипт удаления без аргумента — отметка «служба работала» терялась, и служба не поднималась обратно.
- Процесс sing-box завершается не мгновенно, и запуск успевал начаться, пока старый процесс ещё жив.
- `procd` несколько секунд отдаёт PID уже завершившегося процесса, и это принималось за чужой процесс.

Обновление через `opkg` и `apk` работает одинаково, вручную перезапускать ничего не нужно.

### Безопасность рантайма

Forkop никогда не трогает sing-box, который ему не принадлежит. Остановка, перезапуск и перезагрузка конфигурации отказываются работать, если владение процессом неоднозначно, — вместо того чтобы снять правила nftables и оставить чужой процесс работать без защиты. При неудачном переходе сохраняется fail-closed политика, а не открытый доступ.

### Подписки без ограничений

**Никаких списков разрешённых провайдеров.** Любая рабочая HTTPS-подписка поддерживается. Проверка сертификатов не отключается ни при каких резервных путях. Поддерживается описание хостов из подписки и корректный разбор `xray_json` — URLTest от провайдера работает как задумано.

### Меньше лишнего

- Вырезан функционал локального сервера.
- Убраны поля и настройки, нужные доле процента пользователей.
- QUIC при первой установке принудительно выключен.
- По умолчанию ставится `sing-box-tiny` — меньше памяти на роутерах с 256 МБ.
- Уменьшено количество жёстко зашитых значений, из-за которых sing-box стартовал с невалидной конфигурацией.

### Мелочи, которые заметны каждый день

- URLTest показывает текущий выбранный узел.
- Карточки Priority свёрнуты по умолчанию и запоминают, что вы раскрыли.
- При скачивании отчёта для поддержки предупреждение о приватных данных в файле.
- В журнале видно, удалось ли автоматическое восстановление после неудачного запуска.
- В Diagnostics и Components корректно определяется установленный вариант sing-box.
- Дополнительные встроенные наборы правил: Blizzard, Sony, Bungie, Riot, Twitch, Nintendo, Epic Games, Valve и другие.
- Установка zapret-manager полностью с зеркала, прямо из интерфейса.

## Возможности

**Маршрутизация.** Прозрачный перехват через TPROXY, FakeIP, режимы включения и исключения, смешанный режим. Списки доменов и подсетей, встроенные и произвольные. Исключение отдельных устройств из маршрутизации по секциям.

**Подключения.** Share-ссылки (`vless://`, `vmess://`, `trojan://`, `ss://`, `socks://`, `hysteria2://`), вставленный JSON, подписки с автообновлением. Каскад подключений, привязка к интерфейсам, URLTest и группы Priority с отбором по странам и регулярным выражениям.

**DNS.** Собственный резолвер sing-box, резервный DNS, автопереключение при отказе, режимы работы с dnsmasq, защита от утечек.

**Обход DPI.** Интеграция с Zapret, Zapret2 и ByeDPI как с отдельными компонентами — установка и обновление из интерфейса.

**Обслуживание.** Обновление Forkop, sing-box и компонентов из LuCI. Автоматическое обновление списков и подписок по расписанию. Диагностика с проверками DNS, FakeIP, маршрутов и подключений. Отчёт для поддержки одной кнопкой.

## Поддержка и совместимость

| | |
|---|---|
| OpenWrt | 24.10 (ipk), 25.12+ (apk) |
| Платформы | Filogic `aarch64_cortex-a53`, Rockchip `aarch64_generic` |
| Ядро | sing-box (по умолчанию tiny), extended как опция |
| Память | работает на роутерах с 256 МБ |

## Обратная связь

Проблемы и предложения — в [Issues](https://github.com/slayer326/forkop/issues). К отчёту полезно приложить файл из **Diagnostics → Скачать отчёт для поддержки**; перед публикацией проверьте его — там могут быть приватные данные.

Лицензия: [GPL-2.0-or-later](LICENSE).
